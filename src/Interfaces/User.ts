export interface User {
    firstName: string;
    lastName: string;
    userId: string;
    email: string;
    role: string;
}

export interface SigninModel {
    email: string;
    password: string;
}

